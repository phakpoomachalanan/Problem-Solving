#include <iostream>

int main(void)
{
    int    candidate, round;
    float* period;
    float  out;
    
    std::cin >> candidate, round;
    period = (float*)malloc(sizeof(float)*candidate);

    

    return 0;
}